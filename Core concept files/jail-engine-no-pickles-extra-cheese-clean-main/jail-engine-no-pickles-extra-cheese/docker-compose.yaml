version: "3.9"
services:
  jl-engine-api:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./billing.sqlite:/app/billing.sqlite
    command: sh -c "uvicorn api_server:app --host 0.0.0.0 --port ${PORT:-8080}"
