### Local Host Adapter

Maps world/player inputs into `CoreInput` and converts `CoreOutput` into a `LocalDecision` DTO for gameplay loops.

* Tools: `local_action` local action performer
* Runtime: `python -m jl_platform.cli.main doctor`
