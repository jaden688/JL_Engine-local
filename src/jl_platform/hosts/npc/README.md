### NPC Host Adapter

Maps world/player inputs into `CoreInput` and converts `CoreOutput` into an `NPCDecision` DTO for gameplay loops.

* Tools: `npc_action` local action performer
* Runtime: `python -m jl_platform.cli.main doctor`
